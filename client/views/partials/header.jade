header.navbar.navbar-inverse.navbar-fixed-top(role="navigation")
  .container-fluid
    .navbar-header
      button.navbar-toggle(type="button" ng-click="isCollapsed = !isCollapsed")
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(ga role="button" ui-sref="Index") NpmGems
    nav.navbar-collapse(collapse="!isCollapsed")
      ul.nav.navbar-nav
        li(ui-sref-active="active")
          a(ga ui-sref="Mapping.List") Browse all mappings

        li(ui-sref-active="active")
          a(ga ui-sref="Mapping.Create") Connect a mapping
      ul.nav.navbar-nav.navbar-right
        li(ng-if="_.id")
          a(ga target="_blank" ng-href="https://github.com/{{ _.username }}")
            img#gravatar(ng-src="{{ _.gravatarId | gravatar }}")
            |  {{ _.displayName }}
        li
          a(ga ng-if="!_.id" href="/users/auth/github" target="_self") Github Login
          a(ga ng-if="_.id" href="/users/sign_out" data-method="delete" target="_self") Sign Out
